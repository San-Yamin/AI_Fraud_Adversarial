# Dataset location

Do not commit PaySim or other downloaded datasets here. Store PaySim in Google Drive and set `PAYSIM_DATASET_PATH` when the configured default is not appropriate.

The original CSV must remain unchanged. `isFraud` is the target and `isFlaggedFraud` must be dropped during preprocessing.
